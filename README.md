# vagrant_wordpress

Stupid simple Vagrant setup for WordPress projects.

These files are meant for you to move into the root of your WordPress project.

Modify puppet/manifests/site.pp, mainly for your database.
Then, run `vagrant up`, and you can access your project from http://localhost:3000
